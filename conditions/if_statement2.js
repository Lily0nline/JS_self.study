/*
In the code below, the if...statement will assign a new value to the variable text only if the condition is met. 
Currently, the condition is missing. Add a condtion to make the console.log() statement true
*/

let text = 'hello'; //declaring a new variable and a string value
/*change code here only*/ if (text === 'hello'){ //(filled the empty space with a condition) if the variable text has the value of 'hello'
    text = text + ' world' //then text will have a new value of the current value added with another string 'world' 
}
console.log(text === 'hello world') //(true) calling to see if the current value for text is now 'hello world' 
